# Build handoffs — wave 2026-08-08

Generated 2026-08-08 by `emit-epic-kickoff.mjs` (the groom skill). **Paste each block as-is** into a
fresh Claude Code session — hand-composing these is how the architecture-lock pass gets summarised
away and the review policy silently reverts.

**Order matters.** #13 Sprint 1 ships `ConfirmDialog` and `FormSection`, which #15 consumes from
`main`. Start #13. #14 is independent and can run in parallel. #16 is scaffolded but **not bet** —
no kickoff until a wave file funds it.

---

## 1 — #13 Component-kit adoption sweep (start here)

```
Read AGENTS.md, Roadmap/WAYS-OF-WORKING.md (especially *Epic-mode builds* and *Review & merge*) and
Roadmap/LEARNINGS.md. Skim team memory.
Then read Roadmap/02-commercial/app-component-kit-adoption/README.md and every sprint file: sprint-1.md, sprint-2.md, sprint-3.md.

You're building the ENTIRE "Component-kit adoption sweep — bring the remaining /app routes onto the design system" epic in EPIC MODE, in one orchestrated run — not
3 independent sprint sessions. The sprint documents are integration, review and rollback
boundaries INSIDE this run; they are not separate engagements. Risk tier: LOW.

## 1. Lock the architecture before any builder starts
This is the single highest-leverage act of the whole run, and it is yours alone. Before delegating
anything, read the epic + sprint docs AND the shipped code and live data they describe, then write into
Roadmap/02-commercial/app-component-kit-adoption/README.md:
- numbered decisions `D1…Dn` — each one verified against the live system, not inferred from the doc;
- a per-sprint **"Build contract (locked by the architect before the builder started)"** section.
Builders CITE those decisions. They never re-derive them, and a paraphrased contract drifts permissive.

The locking pass is not a summary of the sprint docs. It must:
- **Disprove scope.** Read the code before believing the doc. An acceptance criterion describing a guard,
  a table, a dependency or a flag state the live system doesn't have is fiction — correct the doc, with
  the reasoning, and say so out loud.
- **Query live data, not just migration files.** Row counts decide what is safe: a schema fork that is
  free while a table is empty is only free then, and that window is worth spending deliberately.
- **Name every deviation** in the README, decided — not discovered by a builder mid-build.
- **Say where each contract lives, once.** Import the shipped rule; never restate it.

## 2. Stack the branches
`feat/app-component-kit-adoption` → `-s2` → `-s3` …, each cut from the previous, one PR per sprint, merged in order.
Sprints in one epic share hot files by construction — siblings cut off one base pay a per-merge conflict
tax. Stack or pay. One PR for the whole epic is acceptable only when the sprints don't split along a
review boundary; prefer per-sprint PRs so the highest-risk sprint gets reviewed as one.

Work in your own isolated `git worktree`, not the shared root checkout. Commit per story PATH-SCOPED
(`git add <your files>` + `git commit -- <those paths>`, never `git add -A`).

## 3. Route the work, and invert it for review
Assign the sprint that defines the contract everything else imports — the authorization boundary, the
migration, the shared seam — to the stronger model; the sprints that are mechanical over a locked
contract go to the faster one. State the routing in the epic README so the choice is auditable.

Escalate rather than guess: stop and ask on payments / checkout / fulfillment / auth / DB migrations /
shared infra / money, plan ambiguity, a decision the plan doesn't cover, or 2+ failed attempts at the same
problem. Default to escalate when unsure. A scope that stops moving is a raised hand, not a reason for
more tokens.

## 4. Review: TWO external families per PR — you do NOT spawn your own reviewers by default
Run the router, don't pick a reviewer by hand:

```
node scripts/review-route.mjs --builder <who-wrote-it> --tier <low|high> <PR#>
```

It applies the policy and prints the exact commands: **two cross-family passes** from the families that
did NOT build the diff (a family never reviews its own work), and nothing else on a LOW-tier PR.

- **LOW tier — do not spawn reviewer subagents.** Two external passes plus the deterministic gate is the
  whole layer. This is a deliberate change: the old habit of running the external passes AND your own
  parallel subagent reviewers on every PR was paying twice for one read.
- **HIGH tier — the fresh reviewer subagent is still mandatory**, on top of the two external passes.
  Money, auth, migrations and shared infra are where context independence catches what every external
  family misses, and that layer stays.
- **If a family is quota-capped, STOP AND ASK ME FOR A REFUND** before substituting your own subagents.
  I can refund external quota in minutes; your subagent tokens come out of this epic's build budget.
  The router prints the exact ask. If I haven't answered within the stated window, proceed with subagents
  and record the downgrade in the PR body — a missing layer must never read like a clean one.

Every finding gets fixed, or answered on the PR with the reason it isn't a bug, before merge. You never
merge your own PR.

## 5. Merging: pre-authorized on green. Done means shipped.
For this epic you are **pre-authorized to merge on a green gate** — deterministic gate green, review
findings resolved, risk tier declared. Don't come back to me for a merge round-trip at each sprint
boundary. Pre-authorization removes the round-trip, not the layers: the gate, the two reviews and the
tier rule all still apply.

It does **not** extend to a new category of production mutation — TLS/IAM/secrets, money or entitlement
writes, a new external dependency or production secret. Name those in one focused question.

**Done means shipped.** A merged PR that hasn't deployed, a migration written but not applied, a flag
that exists in code but not in the flag provider — none of those are done. Apply migrations BEFORE
merging (merging deploys, and code reading a new column against an unmigrated table breaks a live path),
verify live, then merge, then confirm the deploy actually succeeded.

## 6. Assume you die mid-flight
Session limits kill whole agent trees. Derive what is derivable; journal only what isn't — the decided
state lives in the epic README's `D1…Dn` and the per-sprint build contracts, so re-entry is cheap. Never
trust a worker's own completion report: verify by re-deriving actual repo state.

## 7. Close it
Write each sprint's SMOKE WALKTHROUGH into its sprint file before calling that sprint done (numbered
steps, one action + one expected result, real URLs; money/auth steps flagged as owed to me by name).
At epic close, run the full epic Definition of Done in the README — retro, poster, memory, LEARNINGS,
`status: shipped`.

---

## The epic: "Component-kit adoption sweep — bring the remaining /app routes onto the design system" (3 sprints, risk LOW)

### Sprint 1: The three missing primitives
_(boundary: sprint-1.md)_

- Story 1.1 — `DataTable`
- Story 1.2 — `ConfirmDialog`
- Story 1.3 — `FormSection` / `Field`

### Sprint 2: Convert the owner-operated routes
_(boundary: sprint-2.md)_

- Story 2.1 — Convert two routes thin, then freeze the API
- Story 2.2 — Convert `destinations` and `experiments`
- Story 2.3 — Convert `flags` and `impact`
- Story 2.4 — Route-conversion inventory

### Sprint 3: Confirm every destructive action
_(boundary: sprint-3.md)_

- Story 3.1 — Inventory every irreversible action
- Story 3.2 — Wire `ConfirmDialog` to every unconfirmed action
- Story 3.3 — Say what stops
```

---

## 2 — #15 Flags visual rule builder (after #13 Sprint 1 merges)

```
Read AGENTS.md, Roadmap/WAYS-OF-WORKING.md (especially *Epic-mode builds* and *Review & merge*) and
Roadmap/LEARNINGS.md. Skim team memory.
Then read Roadmap/01-growth-engine/flags-visual-rule-builder/README.md and every sprint file: sprint-1.md, sprint-2.md, sprint-3.md.

You're building the ENTIRE "Flags — a visual rule builder, rollout viz, and a plain-language version diff" epic in EPIC MODE, in one orchestrated run — not
3 independent sprint sessions. The sprint documents are integration, review and rollback
boundaries INSIDE this run; they are not separate engagements. Risk tier: HIGH.

## 1. Lock the architecture before any builder starts
This is the single highest-leverage act of the whole run, and it is yours alone. Before delegating
anything, read the epic + sprint docs AND the shipped code and live data they describe, then write into
Roadmap/01-growth-engine/flags-visual-rule-builder/README.md:
- numbered decisions `D1…Dn` — each one verified against the live system, not inferred from the doc;
- a per-sprint **"Build contract (locked by the architect before the builder started)"** section.
Builders CITE those decisions. They never re-derive them, and a paraphrased contract drifts permissive.

The locking pass is not a summary of the sprint docs. It must:
- **Disprove scope.** Read the code before believing the doc. An acceptance criterion describing a guard,
  a table, a dependency or a flag state the live system doesn't have is fiction — correct the doc, with
  the reasoning, and say so out loud.
- **Query live data, not just migration files.** Row counts decide what is safe: a schema fork that is
  free while a table is empty is only free then, and that window is worth spending deliberately.
- **Name every deviation** in the README, decided — not discovered by a builder mid-build.
- **Say where each contract lives, once.** Import the shipped rule; never restate it.

## 2. Stack the branches
`feat/flags-visual-rule-builder` → `-s2` → `-s3` …, each cut from the previous, one PR per sprint, merged in order.
Sprints in one epic share hot files by construction — siblings cut off one base pay a per-merge conflict
tax. Stack or pay. One PR for the whole epic is acceptable only when the sprints don't split along a
review boundary; prefer per-sprint PRs so the highest-risk sprint gets reviewed as one.

Work in your own isolated `git worktree`, not the shared root checkout. Commit per story PATH-SCOPED
(`git add <your files>` + `git commit -- <those paths>`, never `git add -A`).

## 3. Route the work, and invert it for review
Assign the sprint that defines the contract everything else imports — the authorization boundary, the
migration, the shared seam — to the stronger model; the sprints that are mechanical over a locked
contract go to the faster one. State the routing in the epic README so the choice is auditable.

Escalate rather than guess: stop and ask on payments / checkout / fulfillment / auth / DB migrations /
shared infra / money, plan ambiguity, a decision the plan doesn't cover, or 2+ failed attempts at the same
problem. Default to escalate when unsure. A scope that stops moving is a raised hand, not a reason for
more tokens.

## 4. Review: TWO external families per PR — you do NOT spawn your own reviewers by default
Run the router, don't pick a reviewer by hand:

```
node scripts/review-route.mjs --builder <who-wrote-it> --tier <low|high> <PR#>
```

It applies the policy and prints the exact commands: **two cross-family passes** from the families that
did NOT build the diff (a family never reviews its own work), and nothing else on a LOW-tier PR.

- **LOW tier — do not spawn reviewer subagents.** Two external passes plus the deterministic gate is the
  whole layer. This is a deliberate change: the old habit of running the external passes AND your own
  parallel subagent reviewers on every PR was paying twice for one read.
- **HIGH tier — the fresh reviewer subagent is still mandatory**, on top of the two external passes.
  Money, auth, migrations and shared infra are where context independence catches what every external
  family misses, and that layer stays.
- **If a family is quota-capped, STOP AND ASK ME FOR A REFUND** before substituting your own subagents.
  I can refund external quota in minutes; your subagent tokens come out of this epic's build budget.
  The router prints the exact ask. If I haven't answered within the stated window, proceed with subagents
  and record the downgrade in the PR body — a missing layer must never read like a clean one.

Every finding gets fixed, or answered on the PR with the reason it isn't a bug, before merge. You never
merge your own PR.

## 5. Merging: pre-authorized on green. Done means shipped.
For this epic you are **pre-authorized to merge on a green gate** — deterministic gate green, review
findings resolved, risk tier declared. Don't come back to me for a merge round-trip at each sprint
boundary. Pre-authorization removes the round-trip, not the layers: the gate, the two reviews and the
tier rule all still apply.

It does **not** extend to a new category of production mutation — TLS/IAM/secrets, money or entitlement
writes, a new external dependency or production secret. Name those in one focused question.

**Done means shipped.** A merged PR that hasn't deployed, a migration written but not applied, a flag
that exists in code but not in the flag provider — none of those are done. Apply migrations BEFORE
merging (merging deploys, and code reading a new column against an unmigrated table breaks a live path),
verify live, then merge, then confirm the deploy actually succeeded.

## 6. Assume you die mid-flight
Session limits kill whole agent trees. Derive what is derivable; journal only what isn't — the decided
state lives in the epic README's `D1…Dn` and the per-sprint build contracts, so re-entry is cheap. Never
trust a worker's own completion report: verify by re-deriving actual repo state.

## 7. Close it
Write each sprint's SMOKE WALKTHROUGH into its sprint file before calling that sprint done (numbered
steps, one action + one expected result, real URLs; money/auth steps flagged as owed to me by name).
At epic close, run the full epic Definition of Done in the README — retro, poster, memory, LEARNINGS,
`status: shipped`.

---

## The epic: "Flags — a visual rule builder, rollout viz, and a plain-language version diff" (3 sprints, risk HIGH)

### Sprint 1: The rule builder
_(boundary: sprint-1.md)_

- Story 1.1 — The basis-points seam, first
- Story 1.2 — `RuleBuilderRow`
- Story 1.3 — The rule card
- Story 1.4 — Create a flag without typing JSON, behind the gate

### Sprint 2: Rollout visualization and the version diff
_(boundary: sprint-2.md)_

- Story 2.1 — `RolloutBar`
- Story 2.2 — Per-environment state at a glance
- Story 2.3 — Plain-language version diff (bounded)

### Sprint 3: Preview as a user
_(boundary: sprint-3.md)_

- Story 3.1 — Evaluate a context, server-side
- Story 3.2 — Say *why*
- Story 3.3 — Reachable from where the question is asked
```

---

## 3 — #14 Analytics visualization: the charting-dependency decision (spike)

A spike ends in a **written decision, not code** — no branch, no build, no merged chart.
Can run in parallel with #13.

```
Read AGENTS.md, Roadmap/WAYS-OF-WORKING.md and Roadmap/LEARNINGS.md. Skim team memory.
Then read Roadmap/00-ideas/seeds/analytics-visualization-layer.md in full.

You're running a SPIKE, appetite S (one session). It ends in a written decision in that seed
file — no branch, no feature merged, no epic sliced.

The question: do we add a charting dependency to apps/web, and if so which — or do we
hand-roll an SVG set against the token system?

Ground it before you reason about it:
- apps/web/package.json (confirm: no recharts/d3/chart.js/visx/plot; lucide-react is the only
  UI runtime dep)
- apps/web/components/ui/FunnelBars.tsx — the existence proof for the hand-rolled option
- references/design/assets/tokens.css and how apps/web/app/globals.css imports it
- scripts/check-design-drift.mjs — SWEPT_ROOTS and the raw-hex ban. This is the SHARPEST
  constraint: evaluate every candidate by how it takes colour from CSS custom properties,
  not by its feature list.
- apps/web/app/app/impact/[projectSlug]/[featureKey]/page.tsx — the real series to test against

Produce the six things the seed's "What the spike must produce" section names, and make the
worked example the HARDEST chart in the inventory (a line chart with axes plus a comparison
series), not the easiest — a hand-rolled answer that only proves the easy case is not an answer.

This decision gates #16's control-vs-treatment impact chart, so land it before that epic starts.

Before the decision lands, surface (do not auto-run) the advisory cross-family read:
  node scripts/cross-panel.mjs Roadmap/00-ideas/seeds/analytics-visualization-layer.md --lens both --agent codex
It never gates and never writes the doc.

Write the decision into the seed, replacing its "What the spike must produce" section, and
stop there for my sign-off. Then regenerate the board: node scripts/build-order.mjs
```

---

## 4 — The board render patch (chore, S)

```
Read Roadmap/00-ideas/seeds/build-order-render-fix.md — it carries the exact diff and the
acceptance checks.

Fixed scope, low risk, one story. scripts/build-order.mjs renders r.priority where it should
render r.build_order; the funnel also sorts by priority string, so #10 sorts before #2.
build_order_num is already emitted by the extractor — no new plumbing.

Do NOT remove `priority`: it now carries the wave label and pairs with underwritten_by
(intent vs. record).

The mutation check is named in the seed's acceptance criterion 4 — blank a queued seed's
appetite, watch build-order.mjs hard-fail, restore it.
```

---

## Owed, not new scope — the signals-loop close-out

`wave-2026-08-06.md` named this as "the first candidate at the next boundary." It wasn't paid
then and it isn't funded now; `wave-2026-08-08.md` recommends running it as reactive/ops.

`Roadmap/01-growth-engine/signals-loop/` has `status: shipped` in its README frontmatter,
a `RETROSPECTIVE.md` that is still the blank template, and `sprint-1.md` reading `⬜ not
started`. `BUILD-ORDER.md`'s status-drift advisory has been reporting the contradiction across
three waves. Appetite S, no shaping needed. **A drift advisory that has been amber for three
waves stops being a signal and starts being wallpaper.**
