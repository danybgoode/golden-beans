# Wave 2026-08-03 — harness: Shape Up integration

Betting table: Miyagi (product owner) + Fable (architect). Decisions recorded from the live
shaping session; sources in `references/shapeup/`.

| Bet | Appetite | Displaced (the opportunity cost) |
|---|---|---|
| Shape Up integration into the harness: appetite/underwriting ontology (seed frontmatter + board enforcement), the betting layer (this folder), classification lanes, hill routing, the outcome-first prose register — landed across golden-beans and the `ways-of-work` plugin | M (one architect session) | Starting the next product epic (signals-loop E4 / CMS spike E6 stayed parked one wave) |
| De-Miyagification of the `ways-of-work` plugin (groom skill + scope-seed + prose prompt lose hardcoded Miyagi specifics) | folded into the above — decided at the table (we were already in the files) | Would otherwise have been its own S bet next wave |

Decisions of record: appetite denominated in sessions (product-owner attention + session context
are the binding constraints, review rounds close behind); hard circuit breaker for M/L, the
escalate-don't-guess trigger is the only breaker in the S lane; betting at wave boundaries, no
fixed calendar; per-wave bet files (this folder).
